<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="AniT.Input.TextBox" TargetType="TextBox">
        <Setter Property="MinHeight" Value="42"/>
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="Background" Value="{DynamicResource AniT.Brush.SurfaceAlt}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AniT.Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource AniT.Brush.TextPrimary}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource AniT.Brush.AccentCyan}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource AniT.Brush.SurfaceSelected}"/>
        <Setter Property="FontFamily" Value="{StaticResource AniT.Font.Family}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="InputSurface"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="11">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="InputSurface" Property="BorderBrush" Value="{DynamicResource AniT.Brush.BorderStrong}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="InputSurface" Property="BorderBrush" Value="{DynamicResource AniT.Brush.AccentCyan}"/>
                            <Setter TargetName="InputSurface" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="InputSurface" Property="Opacity" Value="0.68"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="InputSurface" Property="Opacity" Value="0.42"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AniT.Input.SearchBox" TargetType="TextBox" BasedOn="{StaticResource AniT.Input.TextBox}">
        <Setter Property="MinHeight" Value="44"/>
        <Setter Property="Padding" Value="42,9,14,9"/>
    </Style>

    <Style x:Key="AniT.Input.ComboBox" TargetType="ComboBox">
        <Setter Property="MinHeight" Value="42"/>
        <Setter Property="Padding" Value="13,8"/>
        <Setter Property="Background" Value="{DynamicResource AniT.Brush.SurfaceAlt}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AniT.Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource AniT.Brush.TextPrimary}"/>
        <Setter Property="FontFamily" Value="{StaticResource AniT.Font.Family}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="AniT.Input.CheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource AniT.Brush.TextSecondary}"/>
        <Setter Property="FontFamily" Value="{StaticResource AniT.Font.Family}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="CheckSurface"
                                Width="18" Height="18"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Background="{DynamicResource AniT.Brush.SurfaceAlt}"
                                BorderBrush="{DynamicResource AniT.Brush.BorderStrong}"
                                BorderThickness="1"
                                CornerRadius="5">
                            <TextBlock x:Name="CheckMark"
                                       Text="✓"
                                       Foreground="White"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                        </Border>
                        <ContentPresenter Grid.Column="1" Margin="7,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckSurface" Property="Background" Value="{DynamicResource AniT.Brush.AccentBlue}"/>
                            <Setter TargetName="CheckSurface" Property="BorderBrush" Value="{DynamicResource AniT.Brush.AccentCyan}"/>
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="CheckSurface" Property="BorderBrush" Value="{DynamicResource AniT.Brush.AccentCyan}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="CheckSurface" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.42"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AniT.Input.Toggle" TargetType="ToggleButton">
        <Setter Property="Width" Value="46"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Border x:Name="ToggleTrack"
                                Background="{DynamicResource AniT.Brush.SurfaceAlt}"
                                BorderBrush="{DynamicResource AniT.Brush.Border}"
                                BorderThickness="1"
                                CornerRadius="13"/>
                        <Ellipse x:Name="ToggleThumb"
                                 Width="19" Height="19"
                                 Margin="3"
                                 HorizontalAlignment="Left"
                                 Fill="{DynamicResource AniT.Brush.TextMuted}"/>
                        <Border x:Name="ToggleFocus"
                                Margin="-3"
                                BorderBrush="{DynamicResource AniT.Brush.AccentCyan}"
                                BorderThickness="2"
                                CornerRadius="16"
                                Opacity="0"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ToggleTrack" Property="Background" Value="{DynamicResource AniT.Brush.AccentBlue}"/>
                            <Setter TargetName="ToggleTrack" Property="BorderBrush" Value="{DynamicResource AniT.Brush.AccentCyan}"/>
                            <Setter TargetName="ToggleThumb" Property="Fill" Value="White"/>
                            <Setter TargetName="ToggleThumb" Property="HorizontalAlignment" Value="Right"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ToggleTrack" Property="BorderBrush" Value="{DynamicResource AniT.Brush.BorderStrong}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="ToggleFocus" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.42"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AniT.Input.Slider" TargetType="Slider">
        <Setter Property="Foreground" Value="{DynamicResource AniT.Brush.AccentCyan}"/>
        <Setter Property="Background" Value="{DynamicResource AniT.Brush.Border}"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>
</ResourceDictionary>
