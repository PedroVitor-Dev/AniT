<UserControl x:Class="AniT.App.GlobalSearchResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="420" MaxHeight="570"
             UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Border Background="#FC071A35" BorderBrush="#4FBFEE" BorderThickness="1.5" CornerRadius="16" Padding="8">
        <Border.Effect><DropShadowEffect Color="#020A18" BlurRadius="26" ShadowDepth="8" Opacity="0.72"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <DockPanel Margin="10,7,10,8">
                <TextBlock x:Name="ResultCountText" DockPanel.Dock="Right" Foreground="#78A9D0" FontSize="10" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⌕" Foreground="#70DFFF" FontSize="18"/>
                    <TextBlock Text="RESULTADOS DA BUSCA" Foreground="#CDEBFF" FontSize="10" FontWeight="Bold" Margin="7,3,0,0"/>
                </StackPanel>
            </DockPanel>

            <Border x:Name="StatusPanel" Grid.Row="1" MinHeight="92" Background="#7810274A" CornerRadius="11" Padding="18">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusGlyph" Text="✦" Foreground="#61D9FF" FontSize="22" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="StatusText" Text="Buscando…" Foreground="#B6D2EB" FontSize="12" HorizontalAlignment="Center" Margin="0,7,0,0"/>
                </StackPanel>
            </Border>

            <ListBox x:Name="ResultsList" Grid.Row="1" ItemsSource="{Binding Results}" Background="Transparent" BorderThickness="0"
                     MaxHeight="440" ScrollViewer.VerticalScrollBarVisibility="Auto" Visibility="Collapsed"
                     PreviewMouseLeftButtonUp="ResultsList_MouseLeftButtonUp">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,1"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="RowSurface" Background="{TemplateBinding Background}" CornerRadius="11" Padding="10,8">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="RowSurface" Property="Background" Value="#173D68"/></Trigger>
                                        <Trigger Property="IsSelected" Value="True"><Setter TargetName="RowSurface" Property="Background" Value="#1C578D"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="54">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="46"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Border Width="38" Height="38" CornerRadius="12" Background="#102E55" BorderBrush="{Binding Accent}" BorderThickness="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Glyph}" Foreground="{Binding Accent}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="3,0,12,0">
                                <TextBlock Text="{Binding Title}" Foreground="White" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Subtitle}" Foreground="#8FB2D2" FontSize="10" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <Border Grid.Column="2" Background="#132D50" BorderBrush="{Binding Accent}" BorderThickness="1" CornerRadius="9" Padding="8,4" VerticalAlignment="Center">
                                <TextBlock Text="{Binding TypeLabel}" Foreground="{Binding Accent}" FontSize="8" FontWeight="Bold"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="2" BorderBrush="#214D77" BorderThickness="0,1,0,0" Margin="3,7,3,0" Padding="8,9,8,3">
                <TextBlock Text="↑ ↓ navegar   •   Enter abrir   •   Esc fechar" Foreground="#698BAD" FontSize="9" HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
